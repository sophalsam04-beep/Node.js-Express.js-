const express = require('express');
const app = express();


// Define a router for the URL
const PORT = 3000;

app.get('/', (req, res) => {
        res.send("Welcome to Express servers...!");     
});



app.listen(PORT, () => {
    console.log("Server is running on port" + PORT);
});